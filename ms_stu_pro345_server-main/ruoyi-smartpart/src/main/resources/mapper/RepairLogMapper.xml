<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.smartpart.mapper.RepairLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ruoyi.smartpart.domain.entity.RepairLog">
        <id column="repair_log_id" property="repairLogId" />
        <result column="repair_order_id" property="repairOrderId" />
        <result column="operation_time" property="operationTime" />
        <result column="status" property="status" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        repair_log_id, repair_order_id, operation_time, status, del_flag
    </sql>
    <select id="selectProList" resultType="com.ruoyi.smartpart.domain.vo.repairlog.RepairLogProVo">
        select
            operation_time,
             status,
             human
        from repair_log
        where repair_order_id = #{repairOrderId}
    </select>

</mapper>
